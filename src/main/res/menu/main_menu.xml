<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <group android:checkableBehavior="all">
        <item android:id="@+id/show_wind_menu"
        android:title="@string/show_wind"
        app:showAsAction="never"/>
        <item android:id="@+id/show_humidity_menu"
            android:title="@string/show_humidity"
            app:showAsAction="never"/>
        <item android:id="@+id/show_pressure_menu"
            android:title="@string/show_pressure"
            app:showAsAction="never"/>
    </group>
</menu>